apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-fetch-service
  namespace: ai-review
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code-fetch-service
  template:
    metadata:
      labels:
        app: code-fetch-service
    spec:
      containers:
        - name: code-fetch-service
          image: ai-code-review-app-code-fetch-service:latest
          imagePullPolicy: IfNotPresent

          envFrom:
            - configMapRef:
                name: ai-review-config
            - secretRef:
                name: ai-review-secrets

          ports:
            - containerPort: 8082

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 180
            periodSeconds: 30
            timeoutSeconds: 5

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 180
            periodSeconds: 30
            timeoutSeconds: 5
