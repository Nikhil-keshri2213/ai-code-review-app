apiVersion: v1
kind: Service
metadata:
  name: code-fetch-service
  namespace: ai-review
spec:
  type: ClusterIP
  selector:
    app: code-fetch-service
  ports:
    - port: 8082
      targetPort: 8082